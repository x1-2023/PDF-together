# Discord App ID (REQUIRED)
# Must match the one in backend
VITE_DISCORD_CLIENT_ID=your_client_id_here

# Backend URLs
# Since we use Caddy reverse proxy, we can use relative paths or the full domain
# For WebSocket, we need the full URL usually, or relative if supported. 
# In production with Caddy handling /ws, we can point to the domain.
VITE_BACKEND_WS_URL=wss://pdf.0xit.me/ws
